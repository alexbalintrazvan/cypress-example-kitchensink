const { Given } = require("@badeball/cypress-cucumber-preprocessor");

Given('Not a real scenario', () => {
    console.log('lol')
});

When('I do something {int} times', (int) => {
  // Write code here that turns the phrase above into concrete actions
})
